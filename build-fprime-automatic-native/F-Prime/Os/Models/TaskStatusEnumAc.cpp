// ======================================================================
// \title  TaskStatusEnumAc.cpp
// \author Generated by fpp-to-cpp
// \brief  cpp file for TaskStatus enum
// ======================================================================

#include <cstring>
#include <limits>

#include "Fw/Types/Assert.hpp"
#include "Os/Models/TaskStatusEnumAc.hpp"

namespace Os {

  // ----------------------------------------------------------------------
  // Operators
  // ----------------------------------------------------------------------

  TaskStatus& TaskStatus ::
    operator=(const TaskStatus& obj)
  {
    this->e = obj.e;
    return *this;
  }

  TaskStatus& TaskStatus ::
    operator=(T e1)
  {
    this->e = e1;
    return *this;
  }

#ifdef BUILD_UT

  std::ostream& operator<<(std::ostream& os, const TaskStatus& obj) {
    Fw::String s;
    obj.toString(s);
    os << s;
    return os;
  }

#endif

  // ----------------------------------------------------------------------
  // Member functions
  // ----------------------------------------------------------------------

  bool TaskStatus ::
    isValid() const
  {
    return ((e >= OP_OK) && (e <= INVALID_STATE));
  }

  Fw::SerializeStatus TaskStatus ::
    serializeTo(
        Fw::SerialBufferBase& buffer,
        Fw::Endianness mode
    ) const
  {
    const Fw::SerializeStatus status = buffer.serializeFrom(
        static_cast<SerialType>(this->e),
        mode
    );
    return status;
  }

  Fw::SerializeStatus TaskStatus ::
    deserializeFrom(
        Fw::SerialBufferBase& buffer,
        Fw::Endianness mode
    )
  {
    SerialType es;
    Fw::SerializeStatus status = buffer.deserializeTo(es, mode);
    if (status == Fw::FW_SERIALIZE_OK) {
      this->e = static_cast<T>(es);
      if (!this->isValid()) {
        status = Fw::FW_DESERIALIZE_FORMAT_ERROR;
      }
    }
    return status;
  }

#if FW_SERIALIZABLE_TO_STRING

  void TaskStatus ::
    toString(Fw::StringBase& sb) const
  {
    Fw::String s;
    switch (e) {
      case OP_OK:
        s = "OP_OK";
        break;
      case INVALID_HANDLE:
        s = "INVALID_HANDLE";
        break;
      case INVALID_PARAMS:
        s = "INVALID_PARAMS";
        break;
      case INVALID_STACK:
        s = "INVALID_STACK";
        break;
      case UNKNOWN_ERROR:
        s = "UNKNOWN_ERROR";
        break;
      case INVALID_AFFINITY:
        s = "INVALID_AFFINITY";
        break;
      case DELAY_ERROR:
        s = "DELAY_ERROR";
        break;
      case JOIN_ERROR:
        s = "JOIN_ERROR";
        break;
      case ERROR_RESOURCES:
        s = "ERROR_RESOURCES";
        break;
      case ERROR_PERMISSION:
        s = "ERROR_PERMISSION";
        break;
      case NOT_SUPPORTED:
        s = "NOT_SUPPORTED";
        break;
      case INVALID_STATE:
        s = "INVALID_STATE";
        break;
      default:
        s = "[invalid]";
        break;
    }
    sb.format("%s (%" PRIu8 ")", s.toChar(), e);
  }

#elif FW_ENABLE_TEXT_LOGGING

  void TaskStatus ::
    toString(Fw::StringBase& sb) const
  {
    sb.format("%" PRIu8 "", e);
  }

#endif

}
