// ======================================================================
// \title  SpiStatusEnumAc.cpp
// \author Generated by fpp-to-cpp
// \brief  cpp file for SpiStatus enum
// ======================================================================

#include <cstring>
#include <limits>

#include "Fw/Types/Assert.hpp"
#include "Drv/Ports/SpiStatusEnumAc.hpp"

namespace Drv {

  // ----------------------------------------------------------------------
  // Operators
  // ----------------------------------------------------------------------

  SpiStatus& SpiStatus ::
    operator=(const SpiStatus& obj)
  {
    this->e = obj.e;
    return *this;
  }

  SpiStatus& SpiStatus ::
    operator=(T e1)
  {
    this->e = e1;
    return *this;
  }

#ifdef BUILD_UT

  std::ostream& operator<<(std::ostream& os, const SpiStatus& obj) {
    Fw::String s;
    obj.toString(s);
    os << s;
    return os;
  }

#endif

  // ----------------------------------------------------------------------
  // Member functions
  // ----------------------------------------------------------------------

  bool SpiStatus ::
    isValid() const
  {
    return ((e >= SPI_OK) && (e <= SPI_OTHER_ERR));
  }

  Fw::SerializeStatus SpiStatus ::
    serializeTo(
        Fw::SerialBufferBase& buffer,
        Fw::Endianness mode
    ) const
  {
    const Fw::SerializeStatus status = buffer.serializeFrom(
        static_cast<SerialType>(this->e),
        mode
    );
    return status;
  }

  Fw::SerializeStatus SpiStatus ::
    deserializeFrom(
        Fw::SerialBufferBase& buffer,
        Fw::Endianness mode
    )
  {
    SerialType es;
    Fw::SerializeStatus status = buffer.deserializeTo(es, mode);
    if (status == Fw::FW_SERIALIZE_OK) {
      this->e = static_cast<T>(es);
      if (!this->isValid()) {
        status = Fw::FW_DESERIALIZE_FORMAT_ERROR;
      }
    }
    return status;
  }

#if FW_SERIALIZABLE_TO_STRING

  void SpiStatus ::
    toString(Fw::StringBase& sb) const
  {
    Fw::String s;
    switch (e) {
      case SPI_OK:
        s = "SPI_OK";
        break;
      case SPI_OPEN_ERR:
        s = "SPI_OPEN_ERR";
        break;
      case SPI_CONFIG_ERR:
        s = "SPI_CONFIG_ERR";
        break;
      case SPI_MISMATCH_ERR:
        s = "SPI_MISMATCH_ERR";
        break;
      case SPI_WRITE_ERR:
        s = "SPI_WRITE_ERR";
        break;
      case SPI_OTHER_ERR:
        s = "SPI_OTHER_ERR";
        break;
      default:
        s = "[invalid]";
        break;
    }
    sb.format("%s (%" PRIu8 ")", s.toChar(), e);
  }

#elif FW_ENABLE_TEXT_LOGGING

  void SpiStatus ::
    toString(Fw::StringBase& sb) const
  {
    sb.format("%" PRIu8 "", e);
  }

#endif

}
