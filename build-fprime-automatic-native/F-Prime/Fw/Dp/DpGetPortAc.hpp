// ======================================================================
// \title  DpGetPortAc.hpp
// \author Generated by fpp-to-cpp
// \brief  hpp file for DpGet port
// ======================================================================

#ifndef Fw_DpGetPortAc_HPP
#define Fw_DpGetPortAc_HPP

#include <cstdio>
#include <cstring>

#include "Fw/Buffer/Buffer.hpp"
#include "Fw/Comp/PassiveComponentBase.hpp"
#include "Fw/FPrimeBasicTypes.hpp"
#include "Fw/Port/InputPortBase.hpp"
#include "Fw/Port/OutputPortBase.hpp"
#include "Fw/Types/String.hpp"
#include "Fw/Types/SuccessEnumAc.hpp"
#include "default/config/FwDpIdTypeAliasAc.hpp"
#include "default/config/FwSizeTypeAliasAc.hpp"

namespace Fw {

  //! Input DpGet port
  //! Port for synchronously getting a data product buffer
  //! Returns the status
  //!
  //! On return, buffer should be set to a valid buffer large enough
  //! to hold a data product packet with the requested data size (if
  //! status is SUCCESS) or an invalid buffer (if status is FAILURE).
  class InputDpGetPort :
    public Fw::InputPortBase
  {

    public:

      // ----------------------------------------------------------------------
      // Constants
      // ----------------------------------------------------------------------

      enum {
        //! The size of the serial representations of the port arguments
        SERIALIZED_SIZE =
          sizeof(FwDpIdType) +
          sizeof(FwSizeType) +
          Fw::Buffer::SERIALIZED_SIZE
      };

    public:

      // ----------------------------------------------------------------------
      // Types
      // ----------------------------------------------------------------------

      //! The port callback function type
      typedef Fw::Success (*CompFuncPtr)(
        Fw::PassiveComponentBase* callComp,
        FwIndexType portNum,
        FwDpIdType id,
        FwSizeType dataSize,
        Fw::Buffer& buffer
      );

    public:

      // ----------------------------------------------------------------------
      // Input Port Member functions
      // ----------------------------------------------------------------------

      //! Constructor
      InputDpGetPort();

      //! Initialization function
      void init();

      //! Register a component
      void addCallComp(
          Fw::PassiveComponentBase* callComp, //!< The containing component
          CompFuncPtr funcPtr //!< The port callback function
      );

      //! Invoke a port interface
      Fw::Success invoke(
          FwDpIdType id, //!< The container ID (input)
          FwSizeType dataSize, //!< The data size of the requested buffer (input)
          Fw::Buffer& buffer //!< The buffer (output)
      );

    private:

#if FW_PORT_SERIALIZATION == 1

      //! Invoke the port with serialized arguments
      Fw::SerializeStatus invokeSerial(Fw::LinearBufferBase& _buffer);

#endif

    private:

      // ----------------------------------------------------------------------
      // Member variables
      // ----------------------------------------------------------------------

      //! The pointer to the port callback function
      CompFuncPtr m_func;

  };

  //! Output DpGet port
  //! Port for synchronously getting a data product buffer
  //! Returns the status
  //!
  //! On return, buffer should be set to a valid buffer large enough
  //! to hold a data product packet with the requested data size (if
  //! status is SUCCESS) or an invalid buffer (if status is FAILURE).
  class OutputDpGetPort :
    public Fw::OutputPortBase
  {

    public:

      // ----------------------------------------------------------------------
      // Output Port Member functions
      // ----------------------------------------------------------------------

      //! Constructor
      OutputDpGetPort();

      //! Initialization function
      void init();

      //! Register an input port
      void addCallPort(
          InputDpGetPort* callPort //!< The input port
      );

      //! Invoke a port interface
      Fw::Success invoke(
          FwDpIdType id, //!< The container ID (input)
          FwSizeType dataSize, //!< The data size of the requested buffer (input)
          Fw::Buffer& buffer //!< The buffer (output)
      ) const;

    private:

      // ----------------------------------------------------------------------
      // Member variables
      // ----------------------------------------------------------------------

      //! The pointer to the input port
      InputDpGetPort* m_port;

  };

}

#endif
